import React from 'react';
import { storiesOf } from '@storybook/react-native';

// Components
import Typography from './index';

storiesOf('Typography', module).add('Standard', () => (
  <Typography>Only for test asd asd</Typography>
));
